import Layout from 'components/Layout';

const Home = () => {
    return (
        <Layout>
            <main>
                <h2>Home</h2>
            </main>
        </Layout>
    );
};

export default Home;
